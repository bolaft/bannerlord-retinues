{% from "_/macros.buttons.j2" import button_sprite %}

{# ---------------------------------------------- #}
{# Statistics                                     #}
{# ---------------------------------------------- #}

<ListPanel
    IsVisible="@OnCharacterPage"
    WidthSizePolicy="CoverChildren" HeightSizePolicy="CoverChildren"
    HorizontalAlignment="Right" VerticalAlignment="Top"
    StackLayout.LayoutMethod="VerticalBottomToTop"
    MarginRight="6">
    <Children>

        {# History button #}
        <ListPanel
            WidthSizePolicy="CoverChildren" HeightSizePolicy="CoverChildren"
            HorizontalAlignment="Right" VerticalAlignment="Center"
            StackLayout.LayoutMethod="HorizontalLeftToRight">
            <Children>

                <TextWidget
                    DataSource="{ShowStatisticsButton}"
                    IsVisible="@IsVisible"
                    WidthSizePolicy="CoverChildren" HeightSizePolicy="CoverChildren"
                    HorizontalAlignment="Right" VerticalAlignment="Center"
                    Brush="Popup.Button.Text"
                    Brush.FontSize="20"
                    MarginRight="8"
                    Text="@Label" />

                {{ button_sprite("{ShowStatisticsButton}", "32", "32", button_brush="DescriptionButtonToggleBrush") }}
            
            </Children>
        </ListPanel>

    </Children>
</ListPanel>
